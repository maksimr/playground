{
  "name": "playground",
  "version": "0.0.0",
  "scripts": {
    "start": "npx -y parcel serve --no-cache --watch-dir=$(pwd) index.html",
    "build": "npx -y parcel build --no-cache --public-url ./ index.html --dist-dir=dist",
    "test": "exit 0"
  },
  "devDependencies": {
    "parcel": "^2.16.1"
  }
}
